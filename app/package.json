{
    "$schema": "https://json.schemastore.org/package.json",
    "name": "optimizely-experiment-builder",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "browser-sync start --server ../page --files '../page/**/*' --no-notify --no-snippet"
    },
    "dependencies": {
        "browser-sync": "^3.0.0",
        "playwright": "^1.60.0"
    }
}
